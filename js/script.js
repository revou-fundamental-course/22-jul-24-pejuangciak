// ini javascript

function formValidation() {
    let Name = document.getElementById('input-Name').value;
    console.log(Name);

    //pengecekan dimana name tidak boleh ''
    if (Name == '') {
        //kode ini akan di eksekusi ketika name= ''
        alert('maaf inputan anda kosong');
    } else {
        //kode ini akan di eksekusi ketika name tidak ''
        alert('sukses menginput');
    }
}

function formValidation() {
    let Email = document.getElementById('input-Gmail').value;
    console.log(Email);

    //pengecekan dimana name tidak boleh ''
    if (Email == '') {
        //kode ini akan di eksekusi ketika name= ''
        alert('maaf inputan anda kosong');
    } else {
        //kode ini akan di eksekusi ketika name tidak ''
        alert('sukses menginput');
    }
}

function formValidation() {
    let Destination = document.getElementById('input-Destination').value;
    console.log(Destination);

    //pengecekan dimana name tidak boleh ''
    if (Destination == '') {
        //kode ini akan di eksekusi ketika name= ''
        alert('maaf inputan anda kosong');
    } else {
        //kode ini akan di eksekusi ketika name tidak ''
        alert('sukses menginput');
    }
}






